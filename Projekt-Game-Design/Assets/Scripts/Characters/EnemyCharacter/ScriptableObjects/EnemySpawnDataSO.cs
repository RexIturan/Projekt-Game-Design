/*
using System.Collections.Generic;
using UnityEngine;

namespace Characters.EnemyCharacter.ScriptableObjects {
    [CreateAssetMenu(fileName = "New EnemySpawnData", menuName = "Character/Enemy/EnemySpawnData")]
    public class EnemySpawnDataSO : ScriptableObject
		{
				public int id;

				public Vector3Int gridPos;
				public List<StatusValue> overrideStatusValues;
				public EnemyBehaviorSO overrideBehavior;

				//todo image id or generate on runtime
				// I think this belongs into enemy type, but we don't need it now anyways
				public int imageID;
				public Sprite profilePicture;
		}
}
*/